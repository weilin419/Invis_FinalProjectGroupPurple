using UnityEngine;
using UnityEngine.SceneManagement;
//This sript is written by Andreea with Kerri's help
public class TEMPLoadSceneAndrea : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void TempLoadScene()
    {
        SceneManager.LoadScene("TheQuiz");
    }
}
